package PermutationsOfArray;

import java.util.ArrayList;

/**
* Problem: Given an array of integers, find all the permutations of the array where one half equals the other half.
 *
 * Solution 1: Brute force, 1) We create every possible permutation of the array.
 *                          2) Test each permutation and see if one half equals the other half.
 * Cons For 1: This will be very time intensive since we will Have to create every permutation of the list, a O(n!)
 *             operation.
*/
public class PermutationsOfArray {
    /**
     *
     * @param arrayOfIntegers
     * @return
     */
    public static ArrayList<ArrayList<Integer>> findEventSplitPermutations(int [] arrayOfIntegers) {
        ArrayList<ArrayList<Integer>> solutions = new ArrayList<>();

       return solutions;
    }
}
